Illustrating working with multiple data sources

1. Install MySQL database server (with admin as the root password)
2. Create two schema with names: first and second
3. Create table called Book with columns ISBN (int & primary key) and TITLE (varchar) in both the schema
4. Run the application 
5. Verify that the record "12345" and "First Book" is created in both first and second schema